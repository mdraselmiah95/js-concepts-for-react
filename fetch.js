//JSON
const student = {
  name: "Shakib",
  age: 54,
  movies: ["not now", "how are you", "king khan"],
};

const studentJSON = JSON.stringify(student);
console.log(student);
console.log(studentJSON);
